# MO2 Control Plane

This subtree holds the MO2 control plane scaffold for agent-facing automation.

- `broker/` contains the broker CLI and protocol helpers.
- `live-bridge/` contains the in-process MO2 Python plugin (`mo2_agent_control.py`),
  its assets-inspector companion, the deploy script, and the broker schema.
